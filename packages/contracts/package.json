{
  "name": "@veilwolf/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Compact contract source for VeilWolf. Not compiled yet — this package is a placeholder for the compactc build output and generated TS bindings once the real chain integration starts.",
  "scripts": {
    "lint": "echo \"[contracts] nothing to lint yet — compactc not wired in\"",
    "typecheck": "echo \"[contracts] nothing to typecheck yet — compactc not wired in\"",
    "test": "echo \"[contracts] no contract tests yet\"",
    "clean": "rm -rf dist .turbo managed"
  }
}
