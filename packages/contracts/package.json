{
  "name": "@veilwolf/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Compact contracts and privacy spikes for VeilWolf.",
  "scripts": {
    "compile": "compact compile --skip-zk private-action.compact managed/private-action",
    "lint": "pnpm run compile",
    "typecheck": "pnpm run compile",
    "test": "pnpm run compile && vitest run private-action.test.ts",
    "clean": "rm -rf dist .turbo managed"
  },
  "devDependencies": {
    "@midnight-ntwrk/compact-runtime": "0.19.0",
    "vitest": "2.1.9"
  }
}
